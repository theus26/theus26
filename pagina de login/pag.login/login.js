function acessar(){
    var nome = (document.getElementById("nome").value);
    var senha = (document.getElementById("senha").value)
    if (nome === "matheusadivir@hotmail.com" && senha === "adivir88") {
        alert("entrou!");
    } else{
        alert("usuario e senha inexistente!");
    }
}

  
